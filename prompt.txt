This is a hackathon project. Along with the code, few documentations are also in this enviroinment. 
While showing demo, I wish to have a 6 distinct user using application. A frontend page asks for roll number and if i give it simply logs in to that user. [no complex.. password stuff.]
Its just demo right. 
Upon seeing page, it has to show the lecture the person has to  revise. For this have 7 lecture notes in OOP , Like lecture 1,2,3.. in the /data/concept file. 
The student can choose one lecture and start revision. 

All the md files in data/concept should have contents related to oops including but not limited to lecture notes [ notes share by faculty for that lecture], transcript of that lecture. 
To keep demo simple, lets stick to 7 leturesin oop alone. 
The model has to look into lecture content, find comman fallacy by its own and strat asking meaningful questions to user. 
Bqacck to frontend, when the user pressses lecture 1, the model can start by asking, Hey! Whats your idea about XXX?,
Then based on users response, the model can ask follow ups and start detecting fallacy. 
Once the entire chat is completed, the model can produce a report of fallacies detected and 
and show on screen about its internal observations and decesions during conversation. 
After it detects the missconcumptionn, It has to store it somewhere. 

Out of 6 students if 3 students has same misconception, call a function named send_email() with message from model, explaaining the status of students. For this for now put some mock terminal printing, i'll look on email code. 

The curerent model is good, but it is using predefined falacy, we wish it to be samrt based on concept uploaded. The current model is asking large questions on the probe follow ups, slightly reduce question size. 

And the current model chaytt never returns in success, so make sure to cut off chat once you ensure  that the person knows concept well to. 

Also while building, i will give permission for your access, use the api token carefully and make not more that 10 api calls while deveoping/testing. 

For now keep the application demo scope as OOP based demo, but the core layer of model should be able to adaptable to all CSE concepts. 

One more thing, It is a hackathon project, so dont think of real users and shi, jsut the mvp is enough but include all features

Also you are such a model eat lot of tokens in my env, so first make a implementation plana nd clear your doubts for the requested chaanges, that i can use  to give as input to other models. 